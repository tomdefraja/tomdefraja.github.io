services:
  jekyll-site:
    image: jekyll-site
    build: .
    volumes:
      - .:/usr/src/app
      - jekyll-gems:/usr/local/bundle  # Persist gems here
    ports:
      - "4000:4000"
    user: 1000:1000
    command: jekyll serve -H 0.0.0.0 -w

volumes:
  jekyll-gems: